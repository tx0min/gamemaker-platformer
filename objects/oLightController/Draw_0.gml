/*surface_set_target(surf);

draw_clear(c_black);

with(oLight){
    draw_set_blend_mode(bm_src_colour);
    draw_sprite_ext(sLight,0,x*other.surfScale,y*other.surfScale,lightSize*other.surfScale,lightSize*other.surfScale,0,c_white,lightStrength);
    
    draw_set_blend_mode(bm_normal);
}
surface_reset_target();
draw_surface_ext(surf,0,0,1/surfScale,1/surfScale,0,c_white,0.8);*/


/* */
/*  */
